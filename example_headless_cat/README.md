# Headless Cat

This is the same as Basic Cat, but runs with headless Chrome

Used by Docker examples:
 - example_docker_cat1
 - example_docker_cat2

Runs also in Docker image robocorp/robocloud-runtime (see https://github.com/robocloud/robocloud-examples/tree/master/example_docker_runtime)
